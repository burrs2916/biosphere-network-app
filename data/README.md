# Data Directory

This directory stores application data files:

- `scan_history.db` - Port scan history database
- `dns_query_history.db` - DNS query history database

These files are automatically created by the application and should not be committed to version control.
